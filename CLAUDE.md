# CLAUDE.md

Read `AGENTS.md` first. In the owner's local workspace, also read
`AI_WORKSPACE/AGENTS.md` and `AI_WORKSPACE/docs/STATUS.md` before making changes.

The live website is entirely under `WEBSITE/`. Never add `AI_WORKSPACE/` to Git.
