# Personal AI workspace: memory, source art, Blender files, workbench tools, and local archives.
/AI_WORKSPACE/

# Website dependencies and generated output.
/WEBSITE/node_modules/
/WEBSITE/dist/

# Secrets — never commit Shopify tokens.
.env
.env.local
/WEBSITE/.env
/WEBSITE/.env.local

# OS/editor and transient files.
.DS_Store
*.log

# Local source backups must never be added to the website repository.
*.blend
*.blend1
*.blend2
