# DECISIONS — locked choices

> Append-only. One line each. A decision here is settled; don't re-litigate it
> without Master Khurram. Format: `YYYY-MM-DD — D<NN>: decision — rationale`.

2026-06-22 — D01: Stack is Vite + Three.js (r155+), GLB/Draco assets — per ba_spec_v2 §3.
2026-06-22 — D02: Both AIs share one protocol (AGENTS.md); no hard task ownership.
2026-06-22 — D03: Comms = live STATUS.md (rewritten) + append-only CHANGELOG/DECISIONS/ASSETS — least tokens, full context.
2026-06-22 — D04: Hard asset gate — no GLB/website use until render approved by Master Khurram.
2026-06-22 — D05: Agents must ask before acting when a prompt lacks detail (AGENTS.md §3).
2026-06-22 — D06: Existing art folders + master .blend stay in place for now (path refs); migration needs approval.

2026-06-22 — D07: First 3D scope = 5 category HERO objects only (not per-product). Products stay 2D image cards until later.
2026-06-22 — D08: Nav categories are now FIVE — added "3D Objects" (= 3D Prints + Lamps + Accessories) as real 3D models.
2026-06-22 — D09: Wall Art 3D = laser-cut look; meshes ALREADY EXIST in master .blend (SVG exported + thickened). Reuse them.
2026-06-22 — D10: Digital Art 3D = flat poster panel, artwork as texture on ONE side.
2026-06-22 — D11: Apparel 3D = dummy placeholder object for now (no real model).
2026-06-22 — D12: The Blender pass is the WHOLE WEBSITE SCENE (environment + staging + lighting + placed hero objects), not just products. The approved render IS the preview of the final website look; web implementation follows the approved scene. Build the scene first.
2026-06-22 — D13: All Blender work lives in the master .blend (black_aesthetics_..._3d_meshes). Organize: website scene as the main scene; each product/product-group in its own Blender COLLECTION; product sets stored as separate SCENES within the same file.
2026-06-22 — D14: NO AI-side renders. The AI builds the scene and sets up the review CAMERA/viewport; Master Khurram inspects the 3D live in Blender himself. Approval still gates the web build, but review happens in-viewport, not via rendered stills.
2026-06-22 — D15: BLENDER LOCK — only ONE agent drives the Blender MCP at a time. The holder is named in STATUS under "🔒 BLENDER LOCK". The other agent must not send Blender commands until released.
2026-06-22 — D16: Current split — CLAUDE owns the Blender scene (environment + 3D). CODEX = website code only, stands down on 3D/scene/renders. Revisit when Claude releases the lock.
2026-06-22 — D17: Gallery = SINGLE refined HERO ROOM (not multi-room/hall). Build straight to detail (skip greybox). Refs = loose inspiration. Hard requirement: must NOT read as a flat cube — use architectural relief (recessed art niches, skylight coffer, floor seams, deep entry portal, wall reveals).
2026-06-23 — D18: Web flow pivots to one approved small-room storefront — categories are physical wall/display locations; browsing zooms into the selected wall area; generated imagery may inform wall textures, but the runtime must read as a 3D room with offset products and minimal luxury UI.
2026-06-23 — D19: D16 superseded — Codex may use Blender when holding the D15 Blender lock; still respect approval gates before GLB export or website asset use.
2026-06-23 — D20: Final visual direction = exact-logo luxury small-room storefront; make one approval image first, then match Blender/web environment to that approved visual.
2026-06-23 — D21: Use `LOGO Blackaesthetics.svg` as-is for brand marks in design and web UI — do not redraw, reinterpret, or replace the logo.
2026-06-23 — D22: Home storefront is a beautiful static composition; free-floating/animated 3D motion starts only in category lists, collection arrays, and product viewer states.
2026-06-23 — D23: Website runs on a hand-curated STATIC catalog of REAL products (test set) with real image textures (posters/wall-art SVG/layered fronts/apparel prints); 3D objects stay procedural until approved GLBs exist. Live Shopify Storefront wiring is optional/later.
2026-06-23 — D24: UNIFIED OPERATOR — the workspace runs as one continuous worker, never running concurrently. **Supersedes D15, D16, D19**: no Blender lock, no file claims, no task ownership. Whichever body is running may use ANY toolset (Blender MCP or website code) freely. STATUS.md is the only sync mechanism. Approval gate (D04, D14) still applies before any asset reaches the website.
2026-06-23 — D25: Gallery environment = faceted 7-wall plan (thick central wall + three 30/60/90° facets per side; perpendicular ends barely in frame), one category staged per facet, warm spotlit ambience per small-room-storefront-exact-logo-v2.png. Built as scene BA_GALLERY; PENDING Master Khurram's in-Blender approval before website port.
2026-06-24 — D26: Apparel is removed from the current design/navigation — no apparel source mesh exists; active storefront has four even zones: Wall Art, Digital Art, Layered Art, 3D Objects.
2026-07-02 — D27: Preferred next website simplification = one continuous wall where home/category/collection/product all happen in the same view; motion comes from camera position, horizontal scroll, and bay/product scale, not separate rooms or different walls.
2026-07-02 — D28: Redesign sequence resets to Blender-first single-wall build — recreate the D27 visual in `BAstore.blend`, get Master Khurram's in-Blender approval, then rebuild/export/wire the website from that approved scene.
2026-07-03 — D29: PROJECT RESET — `BAstore.blend` (scenes `BA_SINGLE_WALL_HOME` + `BA_PRODUCTS`) is the SOLE source of truth; all previous gallery versions, old master .blends, backups, the old website (src/dist/node_modules/models), QA renders, and stale docs were deleted. Product images in `public/products/` and source art folders kept.
2026-07-03 — D30: No QA render PNGs are kept on disk — delete after viewing; review happens live in the Blender viewport.
2026-07-03 — D31: `docs/ARCHITECTURE.md` is the authoritative geometric memory of the gallery (both Blender and web layout contract). It must be updated in the same session as any geometry change; its §9 invariants (frame-box shelves, inner-edge underlights, flushness law, light-gray wall/black frame contrast, one-piece rail fixtures, big-bay/grid alignment) must never regress.
2026-07-03 — D32: Codex Blender MCP uses a stable patched launcher at `/Users/mac/.codex/tools/blender-mcp-lab/bin/blender-mcp` instead of `uvx blender-mcp` — uv cache rotation can drop the Blender 5.1 lab null-byte protocol patch.
2026-07-03 — D33: Layered Art web source = Blender-approved SVG cut-layer exports, not raw GLB meshes — lighter, preserves per-layer cut geometry, and lets Three.js control thickness/depth while Blender remains the source of layer membership/order.
2026-07-03 — D34: Web lighting uses Blender-faithful visible fixtures plus limited real WebGL lights and additive wall pools — too many simultaneous real spot/shadow uniforms caused browser shader failures.
2026-07-03 — D35: Browser-safe existing STL product files may load as product data in the web viewer; oversized 3D print STLs remain procedural stand-ins until optimized or approved as web assets.
2026-07-03 — D36: Digital Art = POSTER, not sticker — the final print file is the front-face texture of ONE black sheet mesh (multi-material box, print on +Z face only, gold corner pins per render target); never a separate image plane floating on a board.
2026-07-03 — D37: WEB COLOR PARITY LAW — every web material/light color is DERIVED from the Blender linear value converted to sRGB (1.055·x^(1/2.4)−0.055), never eyeballed; warm key (1,0.72,0.45)→#ffddb3, plaster→#c0aa89, bay gray→#cecdc9, ash ramp→#424242–#6a6a6a, floor→#5f4632, gold emission→#ffce90. Recompute from the .blend when Blender changes.
2026-07-03 — D38: Heavy 3D-print STLs are decimated to ≤~26k tris via headless Blender (`scripts/optimize_3d_prints.py`, spawned `-b --factory-startup`, sources untouched) into `public/products/3d/` — decimated STLs are product DATA (like poster images/SVGs), not gated authored assets.
2026-07-03 — D39: LIGHTING REMODEL — spotlights exist ONLY above each display area (logo + 4 category bays, 5 fixtures at x −5.25/−3.35/−1.25/0.85/2.95), no roof-wide fixture row; general illumination raised (warm world ambient + stronger fills) so the full view reads open; home cameras reframed (Blender x −1.15, web x −1.05) so the logo fits fully; BA_MAT_ASH_WOOD_DARKGREY rebuilt with natural noise-streak grain (regular wave pattern looked unnatural). Applies to BOTH Blender and web.
2026-07-03 — D40: NO VISIBLE SPOTLIGHT FIXTURES — the hanging can/lens meshes are deleted in Blender and web; each display keeps its invisible SPOT light; the black rail + stems stay as architecture. Web brightness raised further (exposure 1.02, hemisphere 0.52, front fill 2.2, lighter wall shade) — the full view must read open, not dark.
2026-07-03 — D41: FULLY LIT ROOM, NO SPOTLIGHTS AT ALL — every SPOT light deleted in Blender (track, per-product, big-bay) and every aimed spot + wall scallop removed on web; illumination = warm world/ambient + cove + large area fills + boosted bay warm pools (Blender: world 0.09/0.075/0.058, cove 400W, fills 520W, bay pools 120W; web: hemisphere 0.85, cove 3.2, front 3.4, floor wash 1.6, near-zero wall shade). Bay strips/pucks/underlights stay — they are bay furniture, not spotlights. Supersedes the spot-drama aspects of D39/D40.
2026-07-07 — D42: Shelf labels are raised front-face text extrusions, not separate nameplates — category/product names mount directly on each bay shelf surface per Master Khurram's architectural change.
2026-07-11 — D43: Wall Art and Digital Art browse subcollection-first — Shopify-style niches are shown before products, then selected niche products scroll on a moving track while the hero bay stays fixed; hover focus uses additive warm pools, not real spotlights, preserving D41.
2026-07-11 — D44: Selection transitions are prefab handoffs: hold camera, move/scale the selected bay, pan, then stagger-reveal the array; drag moves to a frontal wall camera and product commerce UI waits for the viewer bay — keeps navigation spatial and restrained instead of hard-cutting scene states.
2026-07-11 — D45: Transition bays remain rigid prefabs with uniform XYZ scale; outgoing bays retreat around their own centers and the correctly proportioned destination prefab takes over at the mount point — prevents grid convergence, geometry intersections, and frame deformation.
2026-07-11 — D46: Only the selected product mesh travels between states; old bays clear in place, an empty destination bay spawns, and the product lands inside it. Direct-wall cameras center the browse array, and transitions use real elapsed time so asset loading cannot freeze or mislabel a handoff.
2026-07-12 — D47: HOME becomes a bayless living product wall based on `docs/inspiration-references/home-view-revision-bayless-v1.png`; four product-type zones hover as complete lift/enlarge groups and highlight the matching bottom-nav item, while category/subcollection/viewer states keep their existing bays. Blender approval remains required before web implementation.
2026-07-12 — D48: Add a minimal editorial Intro Home before the D47 product wall, rename D47 to Browse Home in the UI, and stage Browse Home→category transitions as hero-first handoffs followed by glyph and bay reveals with input locked until settle — separates brand introduction from shopping and prevents visible loading/intersection chaos.
2026-07-12 — D49: Remove visible bays from the full web storefront — category, subcollection, grid, and viewer states use bayless wall mounts with products and thick extruded vector text directly on the plaster wall, while invisible hit/transition groups preserve navigation.
2026-07-12 — D50: Web floor becomes black-and-gold marble — replace dark wood/plank floor texture with procedural black marble slabs, grey veins, and gold veining/flecks for a more premium gallery base.
2026-07-12 — D51: Product viewer meshes sit forward off the wall — viewer products mount at about z `0.48` so orbit rotation has clearance and does not overlap the wall plane.
2026-07-12 — D52: 3D Objects use floating shelves in bayless views — keep the bayless storefront, but display object products on black floating shelves offset from the wall in category grids and product viewer so physical pieces have a surface.
2026-07-12 — D53: Product variants — matched products mirror live Shopify data exactly (sizes/prices/variant GIDs via generated shopifyVariants.js); Wall Art + Digital Art additionally get site-side Thickness 2mm/3mm/5mm and Material Wood/Acrylic, priced Acrylic +30% and 5mm +20% — Shopify untouched until Master Khurram approves pushing the new options live.
2026-07-12 — D54: Viewer variant representation — the viewer mesh visually represents the selected variant (size/thickness/material) and the display default is the largest offered size; Wood + 2mm is the modeled visual baseline.
2026-07-14 — D55: Mobile stage — portrait (real ≤760px or Mobile preview) hides the gallery architecture entirely (wall/floor/track/atmosphere/wall logo/3D text) and floats 3D products over a dark charcoal-grey studio backdrop; ALL portrait text is floating DOM UI (data-mobile attr drives it), 3D is reserved for products — chosen by Master Khurram because architecture cropped badly at 9:16 and objects blended with the floor. Desktop keeps the full gallery. One responsive codebase, no separate mobile build.
2026-07-16 — D56: BAstore Blender source is shared through Git LFS — `BA All DATA/All Multilayer Art-3/BAstore.blend` stays at its canonical path and is tracked as an LFS binary so designers can pull/edit/commit/push the same source while backup `.blend1/.blend2` files and other heavy source art remain ignored.

---
## ⏳ Open (from ba_spec_v2 §16 — decide before deep build)
- Hosting: Vercel standalone vs Shopify Hydrogen?
- All product GLBs modeled in Blender, or some photo-textured simple meshes?
- Loading screen: progress bar vs BA logo only?
- Zone 3 sub-collection filter granularity (Wall Art has 10+)?
- Layered art: animate layer reveal in product detail?
