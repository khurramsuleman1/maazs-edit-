# CHANGELOG — append-only history

> One line per change. Format: `YYYY-MM-DD — [agent] — what changed`.
> This is the long-term record. STATUS.md holds the present; this holds the past.

2026-06-22 — [Claude] — Initialized workspace: folder structure, AGENTS.md/CLAUDE.md protocol, docs comms system (STATUS/DECISIONS/ASSETS/CHANGELOG), Vite+Three.js scaffold, git init.
2026-06-22 — [Claude] — Connected Shopify Admin via MCP (Black Aesthetics, PKR, Basic plan). Storefront API token still needed for live site.
2026-06-22 — [Claude] — Pulled 37 collections; wrote docs/CATALOG.md (collection→nav-category mapping + on-disk source files). Per-product detail fetched on demand.
2026-06-22 — [Claude] — Locked D07–D11: 5 nav categories (+3D Objects), 3D treatment per category, first build = 5 hero objects. Blender MCP not yet connected (timed out).
2026-06-22 — [Claude] — Locked D12–D16: scene-first pass, master .blend org, no AI renders (review in Blender), Blender single-agent lock (Claude holds it), Codex→website code only.
2026-06-22 — [Claude] — Rebuilt BA_WEBSITE scene to match Codex inspiration refs: bright pale gallery, archway façade with "BLACK AESTHETICS", real black laser-cut panels (hero=lines_metal_wall_art), white plinths + black objects, planters/trees, track lighting. Staged hero camera for in-Blender review (no render, D14).
2026-06-22 — [Claude] — v2 still read as a flat cube. Stopped building; wrote docs/3D_EXECUTION_PLAN.md (staged blockout→detail→materials→lighting→staging→export, multi-angle review cams, web mapping). Awaiting direction before Stage A blockout.
2026-06-22 — [Claude] — D17 locked (single refined hero room, build-to-detail, loose refs). Rebuilt BA_WEBSITE with real depth: 3 recessed art niches (hero=lines_metal_wall_art), side-wall reveals + doorway to glowing 'beyond', deep entry portal, coffered ceiling + skylight, plinths/objects/apparel dummy, 1 planter, skylight+track-spot lighting. Added 8 review cameras (front/34L/34R/left/right/top/walk + hero). Viewport on hero cam, material shading. Staged for review (D14).
2026-06-22 — [Claude] — Added BA logo (from LOGO svg, black) above the entry portal. Staged all 5 category reps in a row (Apparel tee / Wall Art panel / Digital poster / Layered 3-layer / 3D object) on plinths + new BA_CAM_CATEGORY framing all 5 at once. Viewport set to category cam.
2026-06-22 — [Codex] — Generated a preview-only 3D storefront website design mockup with imagegen; no web assets or code changed.
2026-06-22 — [Codex] — Generated preview-only mobile 9:16 and desktop 16:9 3D storefront mockups with higher contrast for black products.
2026-06-22 — [Codex] — Saved four 3D-world inspiration references for home/category web+mobile under docs/inspiration-references/.
2026-06-23 — [Claude] — Locked guided 4-step flow (Hero→Categories→Collections array→Product array→Product viewer); arrays = horizontal curved carousel (vertical on mobile). Asset strategy per category: WallArt=live SVG extrude / existing meshes, Digital=image panel, Apparel=1 mesh+texture, Layered=Z-stacked layers, 3D=real mesh. (pending DECISIONS entry)
2026-06-23 — [Claude] — Built BA_GALLERY scene (faceted gallery matching small-room-storefront-exact-logo-v2.png): thick central wall + 3 facets/side at 30/60/90°, dark reflective floor, cream walls, warm cove + per-facet spots. Staged BA logo (central), Wall Art laser-cut Athena/Wolf/Lion (L1), Digital posters Ironman/Joker/Naruto (R1), Layered Bear/Mandala (R2), Apparel tees+plinth (L2), 3D objects on plinths (R3), accent planter (L3), centre plinth/vase. Review cam BA_GAL_CAM 1920×1080. Saved. Awaiting in-Blender approval (gate, D14).
2026-06-23 — [Claude] — Locked D23/D24/D25 in DECISIONS.md: D24 (unified operator) explicitly supersedes the Blender lock (D15/D16/D19) so either body uses Blender freely; D23 (static real-product website), D25 (faceted gallery pending approval). Closes the gap where DECISIONS still mandated the lock.
2026-06-23 — [Claude] — Unified operating mode: rewrote AGENTS.md so the workspace runs as one continuous operator (no Blender lock, file claims, ownership split, or to-CODEX messaging); kept read-STATUS→work→update loop + asset gate. Slimmed STATUS accordingly.
2026-06-23 — [Claude] — Wired real products into Codex's website (D23): replaced placeholder catalog in storefront.js with real test set (Apparel×3, WallArt×5, Digital×5, Layered×4, 3DObjects×5) + PKR prices + image paths; copied real artwork to public/products (digital downscaled ≤1600px); environment.js now textures posters/wall-art silhouettes/layered fronts/apparel prints from real images, 3D objects stay procedural. Syntax + image paths verified.
2026-06-23 — [Claude] — Blender crashed on heavy STLs (master .blend). Built lightweight pipeline-test set in BA All DATA/All Multilayer Art-3/All Multilayer Art.blend: 5 SVG laser-cut wall art (Athena/Astronaut/Lion/Wolf/Elephant, solidified+black steel), 5 image posters, 2 small real STLs (panther, fidget axis) + 3 basic-polygon 3D placeholders (cube/sphere/cone). Layered art = 5 native products here WITH real layer geometry (resolves earlier missing-layers flag). Added BA_TEST_CAM. ⚠ file not yet saved. Apparel still a gap.
2026-06-23 — [Claude] — Arranged full catalog in master .blend (scene "Scene"): created 3 product collections; imported all 22 STLs → BA_3D_OBJECT_PRODUCTS (scaled, floored, named); built 33 Digital panels from Final PrintDigital → BA_DIGITAL_ART_PRODUCTS (image-textured); placed 4 Layered placeholders (front-image only, real layer geometry MISSING) → BA_LAYERED_ART_PRODUCTS. Wall Art (148) already present. Added BA_REV_PRODUCTS review cam. No render (D14). Apparel = gap (no base shirt mesh / no source data).
2026-06-23 — [Codex] — Implemented D18 small-room storefront prototype: 5 clickable category wall locations, category zoom/product walls, focused 3D product viewer, search/filters, luxury overlay UI, and build verification.
2026-06-23 — [Codex] — Added category focus behavior: selected wall zones scale up on zoom, category walls render banner-style messages, product previews animate larger, and the outer room shell fades/hides during focus to reduce render work.
2026-06-23 — [Codex] — Superseded D16 with D19 (Codex may use Blender under lock), locked D20/D21 exact-logo small-room direction, copied exact logo to public UI, and created `small-room-storefront-exact-logo-v2.png` approval candidate.
2026-06-23 — [Codex] — Updated website runtime toward `small-room-storefront-exact-logo-v2.png`: warmer showroom shell, in-scene exact logo, ceiling/floor light strips, category wall labels, apparel niche, shelf-mounted product zones, object shelves, and darker glass bottom rail.
2026-06-23 — [Codex] — Locked D22 and updated runtime behavior: home storefront remains static/composed, while free-floating list/product animation starts only in category arrays and product viewer states.
2026-06-23 — [Codex] — Added `docs/BLENDER_SMALL_ROOM_BUILD_BRIEF.md` for Claude: exact visual target, tilted-wall floor plan, wall/floor material specs, category staging, environment messages, navigation-only UI rules, product mesh sources, and review camera checklist.
2026-06-24 — [Codex] — Refined Blender `BA_GALLERY` toward `small-room-storefront-exact-logo-v2.png`: sharper wall edges, polished floor/seams, side bays/shelves, warm cove lighting, cleaner product arrangement, and wider `BA_GAL_CAM`; still staged for in-Blender approval.
2026-06-24 — [Codex] — Diagnosed Blender MCP failure: Blender 5.1 lab add-on expects null-byte-delimited `execute` requests, while wrapper sent old raw `execute_code`; patched local wrapper and confirmed Blender responds directly, but Codex MCP transport needs connector reload.
2026-06-24 — [Codex] — Reduced `BA_GALLERY` wall widths in current Blender scene: central wall now ~2.66m, total frame-fit room ~9.92m, products compressed/recentered, `BA_GAL_CAM` verified to contain all checked walls/objects within x 0.1297–0.8609 and y 0.2724–0.7859.
2026-06-24 — [Codex] — Tightened `BA_GAL_CAM` for `BA_GALLERY` so selected/outer walls are barely inside frame: lens ~15.8mm, verified bounds x 0.0125–0.9752 and y 0.2003–0.8764 with zero behind-camera points.
2026-06-24 — [Codex] — Locked D26 four-category design direction removing Apparel; generated new four-category website/Blender reference images under `docs/inspiration-references/4cat-*`; updated build brief/assets/status and added `docs/MCP_CONNECTION_NOTES.md`.
2026-06-24 — [Codex] — Built `BA_4CAT_WALL_BUILD` in Blender `BA_GALLERY`: four even category wall zones, narrow center logo slab, Apparel hidden, outer returns barely in `BA_GAL_CAM` frame (bounds x 0.0143–0.9857, y 0.2316–0.8198).
2026-06-24 — [Codex] — Added Blender environment/material pass and review renders for `BA_GALLERY` against `4cat-home-web-16x9-v1.png`; latest render `docs/review-renders/ba_4cat_env_final_readable.png` has closer shell/framing but remaining label/logo mirroring to fix.
2026-06-24 — [Codex] — Rebuilt `BA_GALLERY` as true-scale `BA_REAL_SCALE_LAYOUT`: center wall 10ft×18ft, display walls 9ft×18ft, side walls 18ft×18ft, measured camera at (0,20,5.5)ft, true-scale Digital/Layered/Adera/3D shelf placeholders, and light grey procedural concrete wall texture.
2026-06-24 — [Codex] — Updated `BA_GAL_CAM` in `BA_GALLERY` true-scale layout to 20ft from the center wall at 7ft height, aimed at center wall COM (0,0,9ft).
2026-06-24 — [Codex] — Revised `BA_REAL_SCALE_LAYOUT` wall perspective: walls now 9ft high, side visible returns shortened to 2ft, camera stays (0,20,7)ft aimed at center-wall COM (0,0,4.5ft), lens fitted to 7.4mm with all wall points in frame.
2026-06-24 — [Claude] — Connected Blender MCP; in `BA_GALLERY`/`BA_REAL_SCALE_LAYOUT` raised all 7 structural walls 6ft→7ft (base kept on floor, loc_z 1.0668) and lifted the 4 category labels +1ft (z 1.905). Lowered `BA_REAL_CAMERA` to 5ft (1.524m), pulled to ~16ft back (y 4.9), aimed at array (0,0.45,1.30), lens 25mm — clean eye-level homepage hero (bright gallery, dark upper band for nav/hero text, floor foreground for CTA). Saved .blend; preview `docs/inspiration-references/BA_HOMEPAGE_7ft_walls_5ft_cam.png`. Awaiting in-Blender approval (gate).
2026-06-24 — [Claude] — Cleaned the master .blend (`All Multilayer Art.blend`): deleted 278 dead-iteration objects + 8 trash collections (BA_GAL_SHELL/LIGHT/ART, BA_REFERENCE_MATCH_REFINEMENT, BA_FRAME_FIT_ROOM, BA_4CAT_WALL_BUILD, BA_4CAT_MESH_LABELS, empty Adera.svg) + spare BA_GAL_CAM, then purged orphans (meshes 478→75, curves 65→4, cameras 15→2, lights 27→3; file 123MB→110MB). KEPT: `BA_GALLERY` = `BA_REAL_SCALE_LAYOUT` (wall-front environment + staged products) + `BA_REAL_CAMERA`; `Scene` = the 8 real product collections (layered art + BA_TEST_WALLART/DIGITAL/3DOBJ). Pre-cleanup backup at `BA All DATA/All Multilayer Art-3/All Multilayer Art.PRE-CLEANUP-backup.blend`. Post-clean render confirms environment unchanged. Saved.
2026-06-24 — [Claude] — BA_GALLERY display build pass: pulled BA_REAL_CAMERA back to (0,5.5,1.524) lens24 to reveal side returns; added BA_REAL_ROOF (lit ceiling above lights) + ceiling fill light; imported BA logo SVG extruded 1in onto center wall (BA_REAL_LOGO_CENTER). Rebuilt all 4 category labels as fresh FONT meshes — un-mirrored (rot pi/2,0,pi+theta), bigger (size .16) + bolder (offset .006). Rebuilt each wall display to match labels: Digital = 2x3ft 5mm acrylic with ironman.jpg front texture (BA_REAL_DIGITAL_ACRYLIC_2x3FT); Layered = mandala from 'Wooden Multilayer 3D Mandala' collection, 6 layers @5mm stacked (BA_REAL_MANDALA_*); Wall Art = Adera.svg extruded 8mm black wood (BA_REAL_WALLART_ADERA_BLACKWOOD); 3D Objects = 5 zig-zag asymmetric 30x30cm 1in grey-wood shelves each w/ small black sphere + one 4ft 2in slab out 1ft w/ 4 larger black spheres. Materials added (acrylic, black/grey/mandala wood, ceiling). Preview: docs/inspiration-references/BA_HOMEPAGE_displays_v2.png. Saved. Awaiting in-Blender approval (gate).
2026-06-24 — [Claude] — Architectural-gallery pass on BA_GALLERY (approved layout kept), matched to 4cat-home/category reference images: warm beige plaster wall material (noise bump), dark polished reflective floor (rough .06, raytraced reflections), warm dim world. Copied floor up as BA_REAL_CEILING_FROM_FLOOR + recessed warm false-ceiling panel + perimeter cove strips (BA_MAT_COVE_WARM) and 2 cove wash lights. Added viewing-template per art wall: grey-wood ledge + warm under-light emission strip (BA_MAT_UNDERSHELF) below each piece, and a warm SPOT pooling on each piece (Wall Art/Digital/Layered/3D/center logo). Mood pass: dimmed ambient washes so spots read as light pools. Preview: docs/review-renders/ba_gallery_architectural_v1.png. Saved. Awaiting in-Blender approval (gate).
2026-06-24 — [Claude] — BA_GALLERY revision per feedback: raised walls 7ft→8ft (labels to z2.12); fixed shelves/ledges that were stranded at world origin (rebuilt flush to walls). Removed 4ft 3D slab; built 6 dark-grey-wood 30x30cm 1in shelves in a triangular wave on the 3D-Objects wall, each with a small black sphere. Replaced mandala with grey Wolf layered piece ('Multilayer 3D Wolf', 4×5mm layers). Switched to non-reflective WOOD palette: warm wood floor + roof; proper full ROOF = floor mesh copied up onto wall tops (BA_REAL_ROOF_SLAB); false-ceiling soffit = perimeter frame with center cutout dropped below roof (BA_REAL_FALSECEIL_*) housing warm cove LED. Rebuilt lighting to match reference: soft warm key + warm cove washes + per-art soft spotlights (pools of light); warmer ambient. Camera pulled to (0,5.05,1.62) lens24 — focused on displays, all labels in frame. Preview: docs/review-renders/ba_gallery_architectural_v2.png. Saved. Awaiting approval (gate).
2026-06-24 — [Claude] — BA_GALLERY refacet + content swap: refolded walls to 15° (inner display), 30° (outer display), 90° side returns (now perpendicular to center wall), edges connected; repositioned all wall-mounted displays/labels/ledges/spots to the new geometry. Wall Art now = 'Elegant Horse Head.svg' 8mm black wood (replaced Adera). Layered = grey Wolf (kept). 3D Objects renamed → '3D ART'; 6 shelves rearranged into a descending left/right staircase (left column fixed, right column stepping out). Softened 3D-wall spot. Preview: docs/review-renders/ba_gallery_faceted_v3.png. Saved. Awaiting approval (gate).
2026-06-24 — [Claude] — BA_GALLERY architectural pass: analyzed space via workbench ortho top/front/left views; computed wall-edge footprint (x[-3.07,3.07] y[-0.29,2.75]). Rebuilt FLOOR + ROOF from scratch as wood slabs matching that footprint (no more overhang); roof bottom flush with 8ft wall tops. Built floating FALSE-CEILING panel inset from walls with a continuous warm perimeter COVE LED following the floor plan. Fixed harsh spotlights by adding diffuse fill (broad soft downlight FILL_MAIN + front fill + cove washes) and reducing spot energy 300→170 / softer blend; lifted warm world ambient. Preview: docs/review-renders/ba_gallery_diffuse_v4.png. Saved. Awaiting approval (gate).
2026-06-24 — [Claude] — BA_GALLERY refinements: rebuilt grey Wolf with origin centered to geometry (was offset off-wall by un-centered origin → invisible); now dark charcoal, ~0.95m, mounted on LEFT_INNER. Rearranged 3D-ART shelves into a clean staggered 2-column brick pattern (was drifting staircase). Lowered all category labels z2.12→1.74 (off the ceiling, above the art). Added floor/wall skirting (BA_REAL_SKIRTING_* dark trim) along every wall base. Brightened cove + tilted camera up (0,5.2,1.72) to reveal the false ceiling. Preview: docs/review-renders/ba_gallery_refined_v5.png. Saved. Awaiting approval (gate).
2026-06-24 — [Claude] — Fixed Wolf layered piece: layers were stacked back-to-front so the solid silhouette (fewest faces) sat in front and hid the detailed filigree layers. Reordered front→back by detail (Curve.001 filigree front → Curve.007 silhouette back), applied dark→light grey gradient and widened layer spacing to 18mm so all 4 layers read with depth (matches public/products/layered/wolf.png). Preview: docs/review-renders/ba_gallery_wolf_v6.png. Saved.
2026-06-24 — [Claude] — BA_GALLERY framing + 3D lighting: reframed camera to (0,7.8,1.22) lens44 aimed (0,0.55,1.08) to fill the frame with the display band (removes top/bottom negative space). Replaced the single broad 3D-wall spot with 6 small individual spotlights, one per shelf aimed at its black sphere. Preview: docs/review-renders/ba_gallery_framed_v7.png. Saved.
2026-06-24 — [Claude] — Workspace doc refresh: consolidated STATUS.md "Now" (stacked daily entries → single current-state summary of the architectural gallery); updated Next (approve → sync website content: Wall Art=horse head, Layered=Wolf, 3D ART label → export/rebuild web scene to faceted gallery).
2026-06-24 — [Claude] — Renamed master .blend → BAstore.blend (save-as; old All Multilayer Art.blend removed, PRE-CLEANUP backup kept). Built product ARRAYS staged hidden behind their display walls: BA_WALLART_ARRAY = 5 wall-art SVGs (Athena/Astronaut/Lion/Wolf/Elephant) imported + extruded 8mm black wood, lined up behind the Wall Art wall behind the horse-head display. BA_LAYERED_ARRAY = Bear + Mandala + Eclipse-Mandala, each rebuilt as a correctly-ordered grouped layer stack (front-detail→back-silhouette, dark→light gradient, centered origins, 12mm spacing) lined up behind the Layered wall behind the Wolf display (18 layer objects). Digital left as-is per instruction. Hero view confirmed clean (arrays hidden). Saved.
2026-06-24 — [Codex] — Marked current BA_GALLERY approved by Master Khurram; synced website to four active zones (Wall Art/Digital Art/Layered Art/3D ART), faceted gallery shell, SVG-extruded wall art, acrylic digital panels, ordered layered stacks, and procedural 3D ART dummies; build + browser smoke passed.
2026-06-24 — [Codex] — Corrected website environment to use the actual approved Blender scene: exported BA_GALLERY/BA_REAL_SCALE_LAYOUT from BAstore.blend to `public/models/ba-gallery-approved.glb`, loaded it with GLTFLoader, tuned orientation/camera/exposure/lights, and verified home render + product flow with no console errors.
2026-06-24 — [Codex] — Updated web rendering to use Blender geometry but not Blender lighting: disabled imported lights, added reference-driven plaster/floor/fixture/emissive material overrides, warmer code-native lighting, full-screen home camera, and non-overlapping reference-style top/search/bottom-dock UI; build + home/viewer browser checks passed.
2026-06-24 — [Codex] — Ran iterative screenshot QA against 4cat reference renders; added environment-map reflections, floor tile/reflection overlay, tuned exposure/contrast, saved QA passes under docs/qa-screenshots, fixed product geometry contracts (Wall Art SVG 5mm, Digital poster 3mm, Layered 3mm cutout stack), and noted true Layered SVG export gap.
2026-06-24 — [Codex] — Replaced Layered Art PNG-plane fallback with real Blender layer mesh GLBs for Wolf, Bear, Mandala, and Eclipse Mandala; wired Three.js loader/normalization/material ordering, kept 3mm layer-thickness metadata, verified Wolf + Bear product viewers and layer animation with browser screenshots/no console errors.
2026-06-24 — [Codex] — Polished home render toward 4cat reference: corrected gallery Layered Art Wolf by hiding broken imported Wolf layers and mounting verified wolf-layered.glb at the approved anchor; removed wall texture repeat seams, reduced imported fixture shadow artifacts, darkened/cooler lighting and floor wash; build + browser screenshot/console check passed, faint imported-wall seam remains for next cleanup.
2026-06-25 — [Codex] — Disabled homepage category collection scenes for now; category rail hover/focus now keeps home mode and highlights/scales the matching approved-gallery wall, product detail path keeps the room visible on the right display wall, Wolf display is re-centered from approved GLB bounds with a temporary layered texture-stack fallback, and roof/light/material tuning was nudged warmer/darker; build + Chrome Playwright hover smoke passed.
2026-06-25 — [Claude] — Re-enabled the right-wall browsing flow: clicking a category (rail or wall) now opens its collection as a floating array staged in front of the right display wall (camera pans to RIGHT_STAGE; products float in per spec §10), selecting a product hands off to the right-wall product viewer, and back cycles product→collection→home. Cleaned home hover (no ghost-display double image; gold frame + camera lean only), nudged home camera up to centre the display band. Fixed CSS bugs: home intro copy was permanently hidden (display:none) and several text colours were dark-on-dark (home copy, category intro) — now light/gold for contrast. Rail highlights the active collection. node --check passes on all modules; full build/visual verify pending on a non-sandbox machine (rollup/esbuild native binaries + npm registry unavailable here).
2026-07-02 — [Codex] — Prepared the website as a local git repository: cleared stale index lock, verified secret/large-file ignores, recorded repo status, and created the initial commit on `main` pending a GitHub remote.
2026-07-02 — [Codex] — Reworked browsing into an endless right-wall bay flow: category filters promote a lead product into a large lit bay with remaining products repeated to the right, product selection/search opens the selected product in the big bay with related product bays beside it, mouse wheel scrolls along the bay lane, and drag orbits the product camera across a clamped ~170° inspection arc; build + browser smoke passed.
2026-07-02 — [Codex] — Generated and saved the D27 single-wall same-view references (`single-wall-same-view-web-16x9-v1.png`, `single-wall-same-view-mobile-9x16-v1.png`): home/category/collection/product should share one continuous wall, with camera/scale/horizontal movement doing the work.
2026-07-02 — [Codex] — Created Blender scene `BA_SINGLE_WALL_HOME` in `BAstore.blend` for the D27/D28 single-wall home redesign: one wall, four category bay instances, reusable `BA_SW_PREFAB_PRODUCT_BAY_SOURCE`, shelf-front name plates, front home review camera, and future tilted scroller camera; marked staged awaiting Master Khurram review and deleted superseded inspiration PNGs.
2026-07-02 — [Codex] — Polished `BA_SINGLE_WALL_HOME` toward the saved single-wall reference: rebuilt the scene with a black ceiling/header band, continuous warm cove light, visible track spot cans, darker plank floor/skirting, more architectural recessed bay prefab parts, stronger shelf-front name plates, localized bay light pools, and cleaned stale single-wall object suffixes.
2026-07-02 — [Codex] — Generated and saved the clear frontal home render-match target `single-wall-home-front-render-target-web-16x9-v1.png`; linked it in `BA_SINGLE_WALL_HOME.active_reference`, `STATUS.md`, `ASSETS.md`, and the inspiration README so Blender polish now has one exact home-page image to match.
2026-07-02 — [Codex] — Reframed `BA_SW_HOME_FRONT_CAMERA_REVIEW` in `BA_SINGLE_WALL_HOME` to show the full single wall at the reference `1672x941` ratio while preserving product/bay geometry ratios; deleted `BAstore.blend1` with approval to free disk space, saved `BAstore.blend`, and verified tracked wall/product bounds fit in camera frame.
2026-07-02 — [Codex] — Captured `BA_SINGLE_WALL_HOME` from `BA_SW_HOME_FRONT_CAMERA_REVIEW` to `docs/review-renders/ba_single_wall_home_camera_review_2026-07-02.png` and ran a visual critique agent against the frontal reference; recorded that the full wall is visible but the shot is too distant, with oversized dark void, undersized bays/products/logo, and flatter lighting/materials.
2026-07-02 — [Codex] — Rebuilt `BA_SINGLE_WALL_HOME` architecture into new collection `BA_SW_REAL_RATIO_REBUILD`: hid the stretched prefab pass, rebuilt the continuous wall/header/floor/bays to measured storefront proportions, enlarged the BA logo zone, uniformly refit category product stand-ins to preserve ratios, pruned superseded QA renders, saved `BAstore.blend`, and kept latest QA capture at `docs/review-renders/ba_single_wall_home_real_ratio_rebuild_latest_2026-07-02.png`.
2026-07-02 — [Codex] — Reset the single-wall rebuild to a shell-first calibration pass: created `BA_SW_PIXEL_CALIBRATION_SHELL`, hid bays/products/logo/old rebuild layers, kept only wall/floor/roof/baseboard, calibrated `BA_SW_HOME_FRONT_CAMERA_REVIEW` by reference pixel bands, saved `BAstore.blend`, pruned stale QA renders, and kept latest shell capture at `docs/review-renders/ba_single_wall_pixel_shell_latest_2026-07-02.png`.
2026-07-03 — [Claude] — Prefab hierarchy + ash frames per Master Khurram: all architecture grouped under PF_* empties for web export (PF_ARCH_SHELL, PF_TRACK with 11 nested fixture prefabs, 4 PF_BAY_HOME_*, PF_LOGO, PF_DESC_PANEL, PF_BAY_BIG, 20 PF_BAY_SMALL_R{r}C{c}, 2 light rigs) — every bay self-contained with a *_SLOT empty at the product mount plane (y −0.18); collection state-toggling unchanged. New procedural `BA_MAT_ASH_WOOD_DARKGREY` applied to all 100 bay frame parts (jambs/caps/shelves); track hardware stays black. Recovered from a stale-matrix parenting corruption by recomputing matrix_parent_inverse (all positions verified restored); gotcha documented in ARCHITECTURE.md §6b; saved.
2026-07-03 — [Claude] — Contrast correction per Master Khurram: main wall reverted to warm plaster (0.53,0.40,0.25); all 25 bay recess back panels (the bay INNER walls) switched to new light-gray `BA_RR_bay_inner_gray` (0.62,0.61,0.585) so black art pops inside black frames; ARCHITECTURE.md contrast law + materials updated; saved.
2026-07-03 — [Claude] — PROJECT RESET (D29–D31): architectural corrections per Master Khurram (light-gray wall/black frames contrast, shelves re-seated as closed frame boxes flush to wall, underlights moved to inner bottom edges replicating his manual fix, big bay aligned to grid rows z 2.91/0.56, nameplates re-seated); BAstore.blend purged to 2 scenes (BA_SINGLE_WALL_HOME + new BA_PRODUCTS library; old gallery/test scenes, prefabs, shell/pixel/v1 layers deleted — 318 datablocks, 178→165MB); disk cleanup deleted old website (src/dist/node_modules/index/vite/scripts/public/models), old master blends, all .blend1 backups, pre-cleanup backup, review-renders, qa-screenshots, stale docs & inspiration refs (~754MB freed; kept public/products, source art, package.json); wrote docs/ARCHITECTURE.md (full geometric memory, logo origin, bay anatomy laws, invariants); rewrote STATUS (fresh start + website direction), ASSETS, README, AGENTS memory table; appended D29/D30/D31. Awaiting in-Blender review of corrected construct.
2026-07-03 — [Claude] — Scroller page state built per Master Khurram: home product lighting softened (spots 45–65W, pools 70W); wall/header/cove/rail/floor extended right to 18m (+2 stems, +4 track fixtures); new `BA_SW_SCROLLER_PAGE` = description column (eyebrow/title/body/price, physical + flush) in old logo zone, 2.0×2.6m big selection bay with 1.7m horse-head, 2×10 small bays with real meshes (SVG wall-arts, Ironman acrylic, Wolf stack, panther/fidget on shelves), row washes + front fill; home bay parts moved into `BA_SW_RR_HOME_BAYS` for collection-level state toggling; scroller camera set (−6.3,−9.8,1.75) 29mm; saved; render-PNG policy = delete after viewing (all session renders removed).
2026-07-03 — [Claude] — Architectural-correctness pass on BA_SINGLE_WALL_HOME per Master Khurram: track rail now header-mounted via 5 stems, 7 one-piece spot fixtures (yoke+tilted can+recessed lens+own spot light) replace floating can/ball pairs; nameplates seated ON bay shelves (15° placards, text 1.5mm proud), underlights moved under shelf lips; all wall items snapped flush by measured mesh bbox (horse/Ironman/Wolf-back-layer to recess −0.1805, labels to wall −0.081, top strips under caps); REAL `LOGO Blackaesthetics.svg` imported → `BA_SW_REAL_LOGO_MARK` (8mm, 1.4m, flush), font stand-ins hidden, seam moved off logo; QA front `ba_single_wall_arch_corrected_2026-07-03.png` + detail shots; saved; STAGED — awaiting review.
2026-07-03 — [Claude] — Full single-wall home build per Master Khurram (full-build-one-review, supersedes shell-first): unhid + retuned `BA_SW_REAL_RATIO_REBUILD` (AgX Punchy colour mgmt, cove/pool/spot energies, darkened floor seams, emission 4.0), created `BA_SW_REAL_PRODUCTS` with REAL meshes in all 4 bays — horse-head blackwood (Wall Art), Ironman 2×3ft acrylic (Digital), Wolf L1–L4 layer stack (Layered), panther + fidget-axis STLs on 2 new in-bay shelves (3D ART) — plus per-bay recess spots + front fill; review cam `(-0.9,-10,1.7)` 31mm; QA `docs/review-renders/ba_single_wall_real_products_2026-07-03.png`; saved; STAGED — awaiting review. Ops: Blender crashed once from a blocking render via execute channel — rebuilt from history; rule: MCP render tool only + save each milestone.
2026-07-03 — [Codex] — Rebuilt the fresh Vite/Three.js site as a game-like single-wall storefront from the BAstore architecture contract: floating UI, four real-product categories, all-products category grids with nameplates, one centered product-viewer bay, SVG wall-art geometry, procedural 3D stand-ins pending approval; fixed Codex Blender MCP to the stable patched lab launcher; build + Playwright smoke passed.
2026-07-03 — [Codex] — Applied Master Khurram's export approval and pivoted Layered Art from raw GLB meshes to Blender-derived SVG cut-layer exports: added exporter script, generated 22 layer SVGs, wired Three.js thick stepped layer stacks, kept final-print digital art on black sheet panels, removed wall-above-bay labels, and verified build + Chrome visual QA.
2026-07-03 — [Codex] — Matched the web runtime closer to the approved `BA_SINGLE_WALL_HOME` architecture/materials/lighting, fixed WebGL shader failures by reducing real spot/shadow count, expanded the catalog to 53 wall-art SVGs, 30 final prints, 5 layered SVG stacks, and 14 3D objects with 7 small STL viewer meshes, then verified build + Chrome visual QA.
2026-07-03 — [Claude] — D41 FULLY LIT ROOM per Master Khurram: deleted ALL 10 SPOT lights in BA_SINGLE_WALL_HOME (5 track + 4 product + big-bay) and the now-empty fixture prefab empties (saved); boosted general rig (world 0.09, cove 400W, fills 520W, bay pools 120W, row washes 240/200W); web dropped every aimed spotlight + the per-display wall scallops, raised hemisphere 0.85 / cove 3.2 / front 3.4, added a broad floor wash, near-zero wall shade; build + QA screenshot pass; ARCHITECTURE §2/§4/§5.3/§10 + STATUS updated.
2026-07-03 — [Claude] — D40 follow-up after Blender crash (file was saved, nothing lost): deleted all 5 spotlight can/lens meshes in Blender (spot lights kept, saved) and removed the web fixture geometry; raised web brightness again (exposure 1.02, hemi 0.52, fill 2.2, lighter shade); fixed the wall logo (SVG has no width/height → rasterize via canvas, TextureLoader rendered nothing); camera pullback 9.65 fits logo + all bays; restarted the dev server (it died with the crash); build + QA screenshot pass; warned in STATUS against blocking QA renders on heavy states.
2026-07-03 — [Claude] — Lighting remodel per Master Khurram (D39, Blender + web): deleted 6 roof track fixtures and re-seated the remaining 5 above the displays (logo + 4 bays); raised general illumination (warm world background, cove 240→320W, front fills ×1.5; web hemisphere/fills up, wall shade lightened); reframed home cameras so the logo fits fully (Blender −0.9→−1.15, web −0.2→−1.05 + pullback 9.4); rebuilt BA_MAT_ASH_WOOD_DARKGREY with natural noise-streak grain (rough varies with streaks, bump from fine grain) and rebuilt the web ash canvas to match (random-walk streaks, plank bands, knots, pore speckle); BAstore.blend saved; ARCHITECTURE §2/§4/§7 + DECISIONS updated.
2026-07-03 — [Claude] — Render-parity + catalog completion per Master Khurram: locked D36 (Digital Art = poster — print file as FRONT-FACE texture of one black sheet mesh + gold pins), D37 (web color parity law: Blender linear→sRGB, all GalleryScene colors recomputed — plaster/bay-gray/ash/floor/warm-key/gold/black-wood/wolf-layers, wolf layer order fixed to dark-FRONT), D38 (8 heavy 3D-print STLs decimated ≤26k tris via headless `scripts/optimize_3d_prints.py` → all 15 3D Objects now real meshes; home 3D bay = 2 recess shelves + panther/fidget). Added render-target light drama (fixture scallops, recess pucks+pools, underlight washes, floor glows, wall shade falloff) and cut light energies to the target mood; iterated 4 headless-Chrome QA rounds vs the render target (PNGs deleted, D30); AGENTS §6 parity section + ARCHITECTURE §8/§10 updated; mac build passes.
2026-07-04 — [Codex] — Opened the rebuilt storefront in the Browser at `http://localhost:5173/`, verified home/category/search/viewer/mobile interactions and clean console/build, and recorded review findings for 127-host mismatch, desktop/camera clipping, category heading clipping, mobile rail discoverability, and mobile viewer composition.
2026-07-04 — [Claude] — Fixed the four 2026-07-04 browser-QA visual issues via two helper passes (3D scene designer + UI/UX designer) and expanded Wall Art 53→148: responsive home dolly `homeCameraZ()` fits the BA logo + all 4 bays at any landscape aspect (16:10 no longer clips; portrait not force-fit); category headings auto-scale to the ~0.95u gap so DIGITAL ART / 3D OBJECTS no longer collide with the big bay; mobile category rail is now a scroll-snap strip with edge-fade affordance (all 5 categories reachable); mobile product panel is now a compact bottom sheet so the product is no longer hidden; copied 95 clean source SVGs into public/products/wall-art and wired them (catalog now 198 products — all extrude clean; Tupac/Wander/Welter's Monkey spot-checked in the viewer). Edits in src/scene/GalleryScene.js + src/styles.css + src/data/catalog.js; JS syntax-checked and live-dev-server Chrome QA clean at desktop 16:10 + mobile; sandbox `npm run build` blocked only by the missing linux rollup binary (runs on mac).
2026-07-04 — [Codex] — Captured the latest storefront as a no-HUD plate via a temporary deleted `capture.html`, generated five temporary UI selection mockups plus a contact sheet in `/tmp/ba-ui-options/`, and restored the Browser to `http://localhost:5173/`.

2026-07-04 — [Codex] — Used the built-in imagegen skill for a follow-up UI concept pass: direct editing of the exact storefront screenshot was rejected, so generated five safer generic gallery-wall UI concepts inline for style/mood selection while keeping the deterministic `/tmp/ba-ui-options/` boards as the exact-render reference.
2026-07-04 — [Codex] — Expanded the selected first/Gallery Dock UI direction into a six-board desktop view set in `/tmp/ba-gallery-dock-views/` covering home, collection, search, gallery product detail, Shopify checkout bridge, and a proper final Shopify product page with variants, quantity, Add to Cart, Buy with Shopify, and WhatsApp ordering; temporary capture page/server removed and Browser restored to localhost.
2026-07-04 — [Codex] — Implemented the selected direction as the live floating HUD in `src/ui/hud.js` + `src/styles.css`: top BA/search/bag/reset controls, bottom dock across views, Shopify-ready product panel with variants/quantity/Add to Cart/Buy with Shopify/WhatsApp, and a floating checkout bridge; `npm run build` passed and Browser QA passed at desktop 1440×900 + mobile 390×844 with clean console logs.
2026-07-04 — [Codex] — Fixed unreadable web shelf-front nameplates by replacing fixed tiny single-line labels with adaptive high-resolution canvas placards in `src/scene/GalleryScene.js` (larger black plates, two-line gold text, dark stroke, warm border) and added a portrait category camera preset so mobile plates do not crop; `npm run build` passed and Browser QA captured home/category/viewer screenshots in `/tmp/ba-nameplate-qa/` with clean console logs.
2026-07-04 — [agent] — Fixed the two broken Layered Art pieces and virtualized the category grid (helper quota was hit, so implemented directly): `createLayeredSvgStack` now detects the solid full-canvas backing sheet by its tiny path-command count and forces it to the BACK regardless of file index (fixes Motorcycle, whose backing sheet sat mid-stack and hid the detail cuts) and widens the warm-gold mandala palette to include Eclipse Mandala; the category product grid is now windowed — `updateCategoryGrid()`/`buildGridBay()`/`disposeGridBay()` build only the ~9 columns within 6.5u of the scroll centre and dispose far ones (per-mesh geometry + non-shared materials only; shared singletons and cached textures preserved), called on build/wheel/resize, so the 148-product Wall Art grid loads fast and scrolls smoothly. Verified live in Chrome: Motorcycle + Eclipse Mandala render correctly, Wall Art scrolls deep with columns building/disposing seamlessly, virtualized-in products stay clickable (Bob Marley viewer opened), zero console errors; Wolf/Bear/Mandala unchanged; JS syntax-checked.

2026-07-07 — [agent] — Removed legacy operator wording from the live protocol docs so the workspace reads as a plain single-operator setup again.
2026-07-07 — [Codex] — Replaced separate web shelf nameplates with D42 raised shelf-front TextGeometry labels in `src/scene/GalleryScene.js`, updated ARCHITECTURE/STATUS/DECISIONS, and verified build + Browser home/category/mobile screenshots with clean console logs.
2026-07-11 — [Codex] — Reviewed live STATUS to identify the next website steps: Master Khurram UI approval first, then category-scroll/search polish and Shopify Storefront wiring after approval.
